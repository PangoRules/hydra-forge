map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream backend {
    server ${BACKEND_HOST};
}

upstream nuxt {
    server ${NUXT_HOST};
}

server {
    listen 80;
    server_name _;

    include /etc/nginx/hydraforge-templates/locations.conf;
}
